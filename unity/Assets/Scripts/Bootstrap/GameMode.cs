namespace Arena.Bootstrap
{
    // Переключатель "Тренировка/Обучение" (запрос пользователя, 21.09) поверх уже
    // существующего первого экрана "Тренировка/Административный режим". Пока это
    // только флаг без разницы в поведении — оба значения ведут по одному и тому же
    // пути (тест навыков -> диалог -> фидбек); "Обучение" получит дополнительные
    // функции позже, когда они будут определены.
    public enum GameMode
    {
        Training,
        Learning
    }
}
